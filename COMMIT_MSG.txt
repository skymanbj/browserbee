feat: improve OpenAI-compatible provider configuration and storage fallback

Enhanced OpenAI-compatible provider management with dual-storage strategy
(local + sync), dynamic host permission requests, and smart model list
URL discovery.

Changes:
- Added chrome.storage.local fallback for OpenAI compatible instances in factory.ts
- Dual-storage save (local + sync) for instance configuration reliability
- Dynamic chrome.permissions.request() for custom API host origins
- Smart model list URL construction supporting /v1 prefix variations
- Provider removal now syncs to both storage layers and notifies runtime
- Removed TokenUsageDisplay from sidepanel (cleanup)
- ProviderSelector uses configManager.getOpenAICompatibleInstances()
- Updated .gitignore to exclude .mimocode/ and upload_* artifacts
